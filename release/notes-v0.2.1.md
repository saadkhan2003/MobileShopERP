# Mobile Shop ERP 0.2.1

Signed Linux desktop release. This version keeps the shop workflows from 0.2.0 and completes the GitHub Releases update path, including the corrected AppImage asset URL.

Use the AppImage for in-app updates. The `.deb` is available for manual installation. Back up your shop database before replacing an older build.
